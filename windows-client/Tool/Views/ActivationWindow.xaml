<Window x:Class="Tool.Views.ActivationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SNA Pro — Software License Activation"
        Height="480" Width="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#0F172A"
        FontFamily="Segoe UI">
    
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24" HorizontalAlignment="Center">
            <Border Width="48" Height="48" CornerRadius="12" Background="#0284C7" HorizontalAlignment="Center" Margin="0,0,0,12">
                <TextBlock Text="⚡" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <TextBlock Text="SNA Pro" FontSize="22" FontWeight="Black" Foreground="#00E5FF" HorizontalAlignment="Center" />
            <TextBlock Text="Software License Activation" FontSize="13" Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Device PC ID Card -->
        <Border Grid.Row="1" Background="#1E293B" CornerRadius="12" Padding="16" Margin="0,0,0,16" BorderBrush="#334155" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="YOUR PC IDENTIFIER (PC ID)" FontSize="10" FontWeight="Bold" Foreground="#64748B" />
                    <TextBlock x:Name="TxtPcId" Text="PC-7F92KQ81-XP48MD9R" FontSize="14" FontWeight="SemiBold" FontFamily="Consolas" Foreground="#E2E8F0" Margin="0,4,0,0" />
                </StackPanel>
                <Button Grid.Column="1" x:Name="BtnCopyPcId" Content="Copy PC ID" Click="BtnCopyPcId_Click"
                        Padding="12,6" Background="#334155" Foreground="#F8FAFC" FontSize="11" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand" VerticalAlignment="Center">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6" />
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>

        <!-- License Key Input Section -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="LICENSE KEY" FontSize="10" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,6" />
            <TextBox x:Name="TxtLicenseKey" Height="42" Padding="12,10" FontSize="13" FontFamily="Consolas"
                     Background="#020617" Foreground="#818CF8" BorderBrush="#475569" BorderThickness="1"
                     VerticalContentAlignment="Center" TextChanged="TxtLicenseKey_TextChanged" />
            
            <!-- Error Banner -->
            <Border x:Name="PnlError" Background="#451A1A" BorderBrush="#991B1B" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,10,0,0" Visibility="Collapsed">
                <TextBlock x:Name="TxtErrorMessage" Text="Invalid license key." Foreground="#FCA5A5" FontSize="12" TextWrapping="Wrap" />
            </Border>
        </StackPanel>

        <!-- Status indicator -->
        <StackPanel Grid.Row="3" VerticalAlignment="Center" Margin="0,0,0,16">
            <TextBlock x:Name="TxtStatus" Text="Status: Not Activated" FontSize="12" Foreground="#94A3B8" HorizontalAlignment="Center" />
        </StackPanel>

        <!-- Action Button -->
        <Button Grid.Row="4" x:Name="BtnActivate" Content="Activate License" Click="BtnActivate_Click"
                Height="44" Background="#4F46E5" Foreground="#FFFFFF" FontSize="14" FontWeight="Bold"
                BorderThickness="0" Cursor="Hand">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="10" />
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
